!!! 5
%html{ html_attrs }
  %head
    %title omelet
    = stylesheet_link_tag    "omelet_ui/application", :media => "all"
    = javascript_include_tag "omelet_ui/application"
    = csrf_meta_tags

  %body
    -# = render :partial => 'layouts/sidebar'
    %article
      -# = render :partial => 'layouts/breadcrumb'
      %header
        #welcome-text
          Hey there Jerad. Need to go
          = link_to "back to the homepage?", "#"
        #return-text
          powered by omelet

      = yield

