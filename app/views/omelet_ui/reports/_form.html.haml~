= simple_form_for @report do |f|
  = f.input :title
  = f.simple_fields_for :app_meta, @report.app_meta do |b|
    = b.input :notes, :as => :text
    = b.input :start, :as => :datetime
    = b.input :end, :as => :datetime
    = b.input :notes2, :as => :text
  = f.submit
