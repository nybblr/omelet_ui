= simple_form_for @template do |f|
  = f.input :title
  = f.simple_fields_for :app_meta, @report.app_meta do |b|
    = b.input :Layout, :as => :text
    = b.input :Status, :as => :string
  = f.submit
